{
  "name": "my-website-2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test:unit": "node --test tests/*.test.mjs",
    "test": "npm run build && npm run test:unit",
    "validate:site": "node scripts/validate-site.mjs",
    "validate": "npm run build && npm run validate:site",
    "verify": "npm run build && npm run validate:site && npm run test:unit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
